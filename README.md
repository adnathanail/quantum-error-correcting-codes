# Quantum error correcting codes

Install dependencies (including dev)
```shell
uv sync
```

Run tests
```shell
uv run pytest
```

Run type-checking
```shell
uv run ty check
```

Run linting
```shell
uv run ruff check
```

Run auto-formatting
```shell
uv run ruff format
```
